# 
eqn.keys <- list(
                 "min (?:number )?equations" = "^\\s*(?:[0-9]+|-Inf)\\s$",
                 "max (?:number )?equations" = "^\\s*(?:[0-9]+|Inf)\\s$",
                 ""
                 )


